spring:
  application:
    name: backenddemo
  datasource:
    url: jdbc:postgresql://localhost:5431/assignment_db
    username: username
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
  data:
    redis:
      host: localhost
      port: 6378
  cache:
    type: redis